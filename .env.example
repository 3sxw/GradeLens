# Copy to .env. Never commit .env.
# Optional: with a key, grading uses Claude; without one, GradeLens runs
# fully offline using the deterministic heuristic grader.
# ANTHROPIC_API_KEY=sk-ant-your-key-here
